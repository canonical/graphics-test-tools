name: graphics-test-tools
version: "22.04"
base: core22
summary: Tools helpful in debugging graphics stacks
description: |
  - drm-info:
    List the capabilities and current settings of this device's DRM
    (Direct Rendering Manager) hardware.
  - glmark2-es2-wayland:
    OpenGL ES 2.0 Wayland benchmark.
  - eglinfo:
    Dump information about the available EGL setup.

grade: stable
confinement: strict

layout:
  /usr/share/libdrm:  # Needed by mesa-core22 on ARM GPUs
    bind: $SNAP/graphics/libdrm
  /usr/share/drirc.d:
    bind: $SNAP/graphics/drirc.d

plugs:
  graphics-core22:
    interface: content
    target: $SNAP/graphics

apps:
  drm-info:
    plugs:
      - opengl
    command: usr/bin/drm_info

  eglinfo:
    plugs:
      - opengl
      - wayland
    command-chain:
      - graphics-core22-wrapper
      - bin/wayland-launch
    command: usr/bin/eglinfo

  glmark2-es2-wayland:
    plugs:
      - opengl
      - wayland
    command-chain:
      - graphics-core22-wrapper
      - bin/wayland-launch
    command: glmark2-wayland-wrapper

parts:
  glue:
    plugin: dump
    source: glue

  mir-kiosk-snap-launch:
      plugin: dump
      source: https://github.com/MirServer/mir-kiosk-snap-launch.git
      override-build:  ${CRAFT_PART_BUILD}/build-with-plugs.sh opengl wayland
      stage-packages:
        - inotify-tools

  drm-info:
    plugin: nil
    stage-packages:
      - drm-info

  eglinfo:
    plugin: nil
    source: .
    stage-packages:
      - mesa-utils-extra

  glmark2:
    plugin: nil
    source: .
    stage-packages:
      - glmark2-es2-wayland

  cleanup:
    after: [drm-info, eglinfo, glmark2]
    plugin: nil
    build-snaps: [ mesa-core20 ]
    override-prime: |
      set -eux
      cd /snap/mesa-core20/current/egl/lib
      find . -type f,l -exec rm -f $SNAPCRAFT_PRIME/usr/lib/${SNAPCRAFT_ARCH_TRIPLET}/{} \;
      rm -fr "$SNAPCRAFT_PRIME/usr/lib/${SNAPCRAFT_ARCH_TRIPLET}/dri"
      rm -fr "$SNAPCRAFT_PRIME/usr/share/"{bug,doc,doc-base,drirc.d,glvnd,libdrm,lintian,man}
